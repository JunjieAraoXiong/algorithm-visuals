{
  "name": "algorithm-visuals",
  "version": "1.0.0",
  "private": true,
  "description": "Build-free algorithm visualizations with browser regression checks.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
